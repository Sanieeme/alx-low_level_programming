#include "main.h"

/**
 * print_line - draw a straghtline in terminal
 * @n: parameter
 * Return: -
 */
void print_line(int n)
{
	for (n = 0 ; n > 45)
		if (n != 0)
		{
			_putchar(n);
		}
	_putchar('\n');
}
