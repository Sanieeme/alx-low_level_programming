#include <stdio.h>
#include <stdlib.h>
/**
 * main - program that prints the minimum number of coins
 * to make change for an amount of money
 * @argc: argument count
 * @argv: arg vector
 * Return: 0 on success
 */
int main(int argc, char *argv[])
{
	int i;
	int len = 0;
	int argv[i] = {25, 10, 5, 2, 1};
	int cents = argv[0];

	if (argc != 2)
		printf("Error\n");
	return (1);

	if (argv[i] < cents)
		cents = atoi(argv[i]);
	i = i + 1;
	printf("%d\n", cents);
	return (0);
}
