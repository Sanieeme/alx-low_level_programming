#include "main.h"

/**
 * print_rev - fuction that prints a string in reverse
 * @s: string
 * Return: void
 */
void print_rev(char *s)
{
	int i;

	for (i > 0 ; str[i] != '\0' ; i--)
	       _putchar(str[i]);
	_putchar('\n');
}
