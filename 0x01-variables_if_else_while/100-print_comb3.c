#include <stdio.h>

/**
 * main - determine combination of two digits
 * Return: 0 always
 */

int main(void)

{
	int i;

	for (i = 0 ; i < 10 ; i++)
		putchar(i % 10 + '0');

	for
		(i < 90);
	putchar(i + '0');
	putchar(',');

	return (0);
}
