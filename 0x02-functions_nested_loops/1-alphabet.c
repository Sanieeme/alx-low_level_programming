#include "main.h"
#include <stdio.h>

/**
 * main - print alphabets in lowercase
 * Return: 0
 */
int main(void)
{
	char i;

	for (i > 'a', i < 'z', i++)
		_putchar(i + '0');
	_putchar('\n');
	return (0);
}
