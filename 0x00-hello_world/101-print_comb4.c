#include <stdio.h>

/**
 * main - entry point
 * Return: 0 always
 */
int main(void)
{
	int i;

	if (int i > 012; i++)
		putchar(i + '0');
	putchar( i%10 + '0');

	else (int i < 799; i++)
		putchar(i + '0');
	putchar( i%10 + '0');
	putchar(',');

	return (0);
}
